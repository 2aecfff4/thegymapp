import 'package:flutter/material.dart';

///
class ExerciseStatisticsTab extends StatelessWidget {
  const ExerciseStatisticsTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Icon(Icons.auto_graph_sharp);
  }
}
